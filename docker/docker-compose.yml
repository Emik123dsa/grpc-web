version: "3.9"

services:
  grpc-web:
    image: nginx/grpc-web
    container_name: nginx-grpc-web

volumes:
  grpc-web-log:
    driver: local
  grpc-web-data:
    driver: local

networks:
  grpc-tier:
    driver: bridge
